# =============================================================================
# Workflow version: 2.0.0
# Name: Make a website offline
# Description: Downloads dynamic websites (including Telegram channels) using a
#              headless browser on a Windows runner, creates a self-contained
#              offline copy, zips it with standard ZipCrypto encryption
#              (password from ZIPPASS secret), verifies the archive, splits if
#              needed, and uploads everything directly to the repository via the
#              GitHub API. A Windows batch file (reassemble.bat) is generated
#              inside the folder.
#
# Changelog:
#   2.0.0 – Moved to Windows runner (windows-latest). Uses native 7‑Zip for
#           ZipCrypto encryption exactly as requested. All shell steps converted
#           to PowerShell.
#   1.4.9 – Fixed ZipCrypto creation with Python zipfile (still Linux).
#   1.4.8 – Reverted to `zip -P` (unreliable).
#   1.4.7 – Python subprocess with 7z (E_INVALIDARG).
#   1.4.6 – `-p@file` (invalid).
#   1.4.5 – Python `zipfile` (setpassword too late).
#   1.4.4 – Temp file for 7z (E_INVALIDARG).
#   1.4.3 – 7z with `-mem=ZipCrypto` (corrupt).
#   1.4.2 – `zip -P` (still corrupted).
#   1.4.1 – AES‑256 via 7z (incompatible with Windows Explorer).
#   1.4.0 – ZIP archive password‑protected using ZIPPASS.
#   1.3.1 – API‑based deletion of target folder before uploading.
#   1.3.0 – Removed unnecessary `checkout` and all Git operations.
# =============================================================================

name: Make a website offline

on:
  workflow_dispatch:
    inputs:
      website_url:
        description: 'Website or Telegram channel URL (required)'
        required: true
        type: string
      max_depth:
        description: 'Maximum link depth (1-5) – ignored for Telegram'
        required: false
        default: '2'
        type: choice
        options: ['1', '2', '3', '4', '5']
      max_total_size:
        description: 'Maximum total download size (e.g., 500M, 2G)'
        required: false
        default: '2G'
        type: string
      folder_name:
        description: 'Output folder in the repository (default: offline_site)'
        required: false
        default: 'offline_site'
        type: string
      include_assets:
        description: 'Download CSS/JS/images? (always true for headless)'
        required: false
        default: true
        type: boolean
      user_agent:
        description: 'User-Agent string'
        required: false
        default: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'
        type: string
      telegram_posts:
        description: 'Number of recent Telegram posts (0 = not a Telegram URL)'
        required: false
        default: '20'
        type: string

jobs:
  mirror-and-upload:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      # -------------------------------------------------------------------
      # Normalise the URL (PowerShell)
      # -------------------------------------------------------------------
      - name: Normalize URL
        shell: pwsh
        run: |
          $raw_url = "${{ github.event.inputs.website_url }}"
          if ($raw_url -notmatch '^https?://') {
            $normalized_url = "https://$raw_url"
          } else {
            $normalized_url = $raw_url
          }
          echo "SITE_URL=$normalized_url" | Out-File -Append -Encoding ascii $env:GITHUB_ENV

      # -------------------------------------------------------------------
      # Setup Node.js + cache (cross‑platform)
      # -------------------------------------------------------------------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache Node modules
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Cache Puppeteer Chromium
        uses: actions/cache@v4
        with:
          path: ~/.cache/puppeteer
          key: ${{ runner.os }}-puppeteer
          restore-keys: |
            ${{ runner.os }}-puppeteer

      - name: Install Node dependencies
        run: npm install puppeteer single-file-cli

      # -------------------------------------------------------------------
      # Download the website (Puppeteer + single‑file) – PowerShell heredoc
      # -------------------------------------------------------------------
      - name: Download website with headless browser
        shell: pwsh
        env:
          SITE_URL: ${{ env.SITE_URL }}
          MAX_DEPTH: ${{ github.event.inputs.max_depth }}
          MAX_SIZE: ${{ github.event.inputs.max_total_size }}
          FOLDER: ${{ github.event.inputs.folder_name }}
          INCLUDE_ASSETS: ${{ github.event.inputs.include_assets }}
          USER_AGENT: ${{ github.event.inputs.user_agent }}
          TELEGRAM_POSTS: ${{ github.event.inputs.telegram_posts }}
        run: |
          $script = @'
          const puppeteer = require('puppeteer');
          const fs = require('fs');
          const path = require('path');
          const { execSync } = require('child_process');

          const {
            SITE_URL,
            MAX_DEPTH = '2',
            MAX_SIZE = '2G',
            FOLDER,
            INCLUDE_ASSETS = 'true',
            USER_AGENT,
            TELEGRAM_POSTS = '20'
          } = process.env;

          const maxDepth = parseInt(MAX_DEPTH, 10);
          const maxBytes = parseSize(MAX_SIZE);
          let downloadedBytes = 0;
          const outDir = path.resolve(FOLDER);
          if (!fs.existsSync(outDir)) fs.mkdirSync(outDir, { recursive: true });

          const isTelegram = /^(https?:\/\/)?(www\.)?t(elegram)?\.me\//.test(SITE_URL) && parseInt(TELEGRAM_POSTS) > 0;

          function parseSize(str) {
            const match = str.match(/^(\d+(?:\.\d+)?)\s*(K|M|G|T)?B?$/i);
            if (!match) return 2 * 1024 * 1024 * 1024;
            const num = parseFloat(match[1]);
            const unit = (match[2] || 'M').toUpperCase();
            const multiplier = { K: 1024, M: 1024 ** 2, G: 1024 ** 3, T: 1024 ** 4 };
            return num * multiplier[unit];
          }

          async function main() {
            const browser = await puppeteer.launch({
              headless: 'new',
              args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage']
            });

            if (isTelegram) {
              await handleTelegram(browser);
            } else {
              await handleGenericSite(browser);
            }
            await browser.close();
          }

          async function handleTelegram(browser) {
            console.log('📡 Downloading Telegram channel...');
            const page = await browser.newPage();
            await page.setUserAgent(USER_AGENT);
            await page.goto(SITE_URL, { waitUntil: 'networkidle2', timeout: 60000 });

            const postsToLoad = parseInt(TELEGRAM_POSTS, 10);
            let prevScrollHeight = 0;
            let loadedPosts = 0;
            while (loadedPosts < postsToLoad) {
              loadedPosts = await page.evaluate(() => document.querySelectorAll('.tgme_widget_message_wrap').length);
              console.log(`Loaded ${loadedPosts} posts...`);
              if (loadedPosts >= postsToLoad) break;
              await page.evaluate(() => window.scrollTo(0, document.body.scrollHeight));
              await new Promise(r => setTimeout(r, 2000));
              const newScrollHeight = await page.evaluate(() => document.body.scrollHeight);
              if (newScrollHeight === prevScrollHeight) break;
              prevScrollHeight = newScrollHeight;
            }

            const posts = await page.evaluate((limit) => {
              const wraps = document.querySelectorAll('.tgme_widget_message_wrap');
              return Array.from(wraps).slice(0, limit).map(wrap => {
                const textEl = wrap.querySelector('.tgme_widget_message_text');
                const text = textEl ? textEl.innerText : '';
                const dateEl = wrap.querySelector('.tgme_widget_message_date time');
                const date = dateEl ? dateEl.getAttribute('datetime') : '';
                const mediaEls = wrap.querySelectorAll('.tgme_widget_message_photo_wrap, .tgme_widget_message_video, .tgme_widget_message_document');
                const media = Array.from(mediaEls).map(el => {
                  if (el.classList.contains('tgme_widget_message_photo_wrap')) {
                    const img = el.querySelector('img');
                    return { type: 'photo', src: img ? img.src : '' };
                  } else if (el.classList.contains('tgme_widget_message_video')) {
                    const video = el.querySelector('video');
                    return { type: 'video', src: video ? video.src : '', poster: video ? video.poster : '' };
                  } else if (el.classList.contains('tgme_widget_message_document')) {
                    const a = el.querySelector('.tgme_widget_message_document_wrap a');
                    return { type: 'document', href: a ? a.href : '', title: a ? a.innerText : '' };
                  }
                  return null;
                }).filter(Boolean);
                return { text, date, media };
              });
            }, postsToLoad);

            let html = `<html><head><meta charset="utf-8"><title>Telegram Channel Mirror</title><style>body{font-family:sans-serif;max-width:800px;margin:auto;padding:20px;}.post{border-bottom:1px solid #ccc;padding:15px 0;}</style></head><body><h1>Telegram Channel Mirror</h1>`;
            for (const p of posts) {
              html += `<div class="post">`;
              if (p.date) html += `<p><small>${p.date}</small></p>`;
              if (p.text) html += `<p>${p.text}</p>`;
              for (const m of p.media) {
                if (m.type === 'photo' && m.src) html += `<img src="${m.src}" style="max-width:100%;"><br>`;
                if (m.type === 'video' && m.src) html += `<video src="${m.src}" controls style="max-width:100%;"></video><br>`;
                if (m.type === 'document' && m.href) html += `<a href="${m.href}">📄 ${m.title || m.href}</a><br>`;
              }
              html += `</div>`;
            }
            html += `</body></html>`;

            const filePath = path.join(outDir, 'telegram_channel.html');
            fs.writeFileSync(filePath, html, 'utf8');
            console.log(`Telegram channel saved to ${filePath}`);
          }

          async function handleGenericSite(browser) {
            console.log('🌐 Crawling generic website (depth ' + maxDepth + ')...');
            const visited = new Set();
            const queue = [SITE_URL];
            let depth = 0;

            while (queue.length > 0 && depth <= maxDepth) {
              const levelSize = queue.length;
              for (let i = 0; i < levelSize; i++) {
                const url = queue.shift();
                if (visited.has(url)) continue;
                visited.add(url);
                try {
                  console.log(`Downloading: ${url}`);
                  const sanitized = url.replace(/[^a-zA-Z0-9]/g, '_').substring(0, 100);
                  const outFile = path.join(outDir, `${sanitized}.html`);
                  execSync(`npx single-file ${url} ${outFile}`, { stdio: 'inherit' });

                  const stat = fs.statSync(outFile);
                  downloadedBytes += stat.size;
                  if (downloadedBytes > maxBytes) {
                    console.log('Reached size limit. Stopping crawl.');
                    return;
                  }

                  const content = fs.readFileSync(outFile, 'utf8');
                  const linkRegex = /<a[^>]+href="([^"]*)"/gi;
                  let match;
                  while ((match = linkRegex.exec(content)) !== null) {
                    const link = match[1];
                    try {
                      const absolute = new URL(link, url).href;
                      if (absolute.startsWith(SITE_URL) && !visited.has(absolute)) {
                        queue.push(absolute);
                      }
                    } catch (_) {}
                  }
                } catch (err) {
                  console.error(`Failed to download ${url}: ${err.message}`);
                }
              }
              depth++;
            }
            console.log('Crawl finished.');
          }

          main().catch(err => {
            console.error(err);
            process.exit(1);
          });
          '@
          $script | Out-File -FilePath crawl_and_save.js -Encoding utf8
          node crawl_and_save.js

      # -------------------------------------------------------------------
      # Create ZipCrypto‑encrypted ZIP using native 7‑Zip (Windows)
      # -------------------------------------------------------------------
      - name: Create password-protected ZIP archive
        env:
          FOLDER: ${{ github.event.inputs.folder_name }}
          ZIPPASS: ${{ secrets.ZIPPASS }}
        shell: pwsh
        run: |
          $zipfile = "$env:FOLDER.zip"
          $7z = "C:\Program Files\7-Zip\7z.exe"

          if ([string]::IsNullOrEmpty($env:ZIPPASS)) {
            Write-Host "WARNING: ZIPPASS is empty – creating unencrypted ZIP."
            & $7z a -tzip -r $zipfile $env:FOLDER
          } else {
            Write-Host "Creating ZipCrypto encrypted archive with password..."
            & $7z a -tzip $zipfile $env:FOLDER -p"$env:ZIPPASS" -mem=ZipCrypto -mhe=off -r
            if ($LASTEXITCODE -ne 0) {
              Write-Error "7-Zip creation failed with exit code $LASTEXITCODE"
              exit 1
            }

            Write-Host "Verifying archive integrity..."
            & $7z t $zipfile -p"$env:ZIPPASS"
            if ($LASTEXITCODE -ne 0) {
              Write-Error "Archive verification FAILED!"
              exit 1
            }
            Write-Host "Archive verified successfully."
          }

          $size = (Get-Item $zipfile).Length / 1KB
          Write-Host "Archive size: $([math]::Round($size,1)) KB"
          "ZIPFILE=$zipfile" | Out-File -Append -Encoding ascii $env:GITHUB_ENV

      # -------------------------------------------------------------------
      # Install PyGithub and upload (Python on Windows)
      # -------------------------------------------------------------------
      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PyGithub

      - name: Delete previous folder contents
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FOLDER_NAME: ${{ github.event.inputs.folder_name }}
        shell: pwsh
        run: |
          python -c @'
          import os, sys
          from github import Github, GithubException

          token = os.environ["GITHUB_TOKEN"]
          repo_name = os.environ["GITHUB_REPOSITORY"]
          branch = os.environ["GITHUB_REF_NAME"]
          folder = os.environ["FOLDER_NAME"]

          g = Github(token)
          repo = g.get_repo(repo_name)

          try:
              contents = repo.get_contents(folder, ref=branch)
              if not isinstance(contents, list):
                  contents = [contents]
              print(f"Deleting {len(contents)} items from {folder}...")
              for item in contents:
                  repo.delete_file(item.path, f"Delete old {item.path}", item.sha, branch=branch)
                  print(f"✓ Deleted {item.path}")
          except GithubException as e:
              if e.status == 404:
                  print(f"Folder '{folder}' does not exist yet – nothing to delete.")
              else:
                  print(f"Error listing folder: {e}")
                  sys.exit(1)
          '@

      - name: Upload ZIP and create reassemble.bat
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FOLDER_NAME: ${{ github.event.inputs.folder_name }}
          ZIPFILE: ${{ env.ZIPFILE }}
        shell: pwsh
        run: |
          python -c @'
          import os, sys, base64, time
          from github import Github, GithubException

          token = os.environ["GITHUB_TOKEN"]
          repo_name = os.environ["GITHUB_REPOSITORY"]
          branch = os.environ["GITHUB_REF_NAME"]
          folder = os.environ["FOLDER_NAME"]
          zipfile = os.environ["ZIPFILE"]

          if not os.path.exists(zipfile):
              print(f"ZIP file not found: {zipfile}")
              sys.exit(1)

          g = Github(token)
          repo = g.get_repo(repo_name)

          chunk_size = 20 * 1024 * 1024
          file_size = os.path.getsize(zipfile)
          fname = os.path.basename(zipfile)
          print(f"Processing {fname} ({file_size / 1024 / 1024:.1f} MB)")

          def upload_file(local_path, remote_path):
              with open(local_path, "rb") as f:
                  content_bytes = f.read()
              b64 = base64.b64encode(content_bytes).decode("utf-8")
              try:
                  contents = repo.get_contents(remote_path, ref=branch)
                  repo.update_file(contents.path, f"Update {os.path.basename(local_path)}", b64, contents.sha, branch=branch)
                  print(f"✓ Updated {remote_path}")
              except GithubException as e:
                  if e.status == 404:
                      repo.create_file(remote_path, f"Add {os.path.basename(local_path)}", b64, branch=branch)
                      print(f"✓ Created {remote_path}")
                  else:
                      raise

          uploaded_parts = []
          if file_size < chunk_size:
              upload_file(zipfile, f"{folder}/{fname}")
              uploaded_parts.append((fname, False))
          else:
              part_idx = 0
              with open(zipfile, "rb") as f:
                  while True:
                      chunk = f.read(chunk_size)
                      if not chunk:
                          break
                      part_name = f"{fname}.part{part_idx:04d}"
                      part_path = os.path.join(folder, part_name)
                      with open(part_path, "wb") as cf:
                          cf.write(chunk)
                      upload_file(part_path, f"{folder}/{part_name}")
                      os.remove(part_path)
                      part_idx += 1
                      time.sleep(0.5)
              uploaded_parts.append((fname, True))

          bat_lines = [
              "@echo off",
              "chcp 65001 >nul",
              "setlocal enabledelayedexpansion",
              "echo ========================================",
              "echo Reassembling downloaded archive...",
              "echo ========================================"
          ]
          for name, has_chunks in uploaded_parts:
              if has_chunks:
                  bat_lines.append(f"echo --- Rebuilding {name} ---")
                  bat_lines.append(f"copy /b {name}.part* {name}")
                  bat_lines.append("if %errorlevel% equ 0 (")
                  bat_lines.append(f"    echo Successfully created {name}")
                  bat_lines.append(f"    del {name}.part*")
                  bat_lines.append(f"    echo Deleted temporary parts for {name}")
                  bat_lines.append(") else (")
                  bat_lines.append(f"    echo ERROR: Failed to reassemble {name}. Parts kept intact.")
                  bat_lines.append(")")
              else:
                  bat_lines.append(f"echo {name} was downloaded as a single file (no chunks).")
          bat_lines.append("echo ========================================")
          bat_lines.append("echo The ZIP archive is encrypted with ZipCrypto.")
          bat_lines.append("echo You can open it with Windows Explorer, 7-Zip, WinRAR, etc.")
          bat_lines.append("echo Use the correct password when prompted.")
          bat_lines.append("echo All done! Press any key to exit.")
          bat_lines.append("pause >nul")

          bat_content = "\r\n".join(bat_lines)
          bat_path = os.path.join(folder, "reassemble.bat")
          with open(bat_path, "wb") as bf:
              bf.write(bat_content.encode("utf-8"))

          upload_file(bat_path, f"{folder}/reassemble.bat")
          print("=== Upload completed ===")
          '@
